{% extends 'base.html.twig' %}

{% block title %}TypeOfVehicule index{% endblock %}

{% block body %}
    <h1>TypeOfVehicule index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Type</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for type_of_vehicule in type_of_vehicules %}
            <tr>
                <td>{{ type_of_vehicule.id }}</td>
                <td>{{ type_of_vehicule.type }}</td>
                <td>
                    <a href="{{ path('type_of_vehicule_show', {'id': type_of_vehicule.id}) }}">show</a>
                    <a href="{{ path('type_of_vehicule_edit', {'id': type_of_vehicule.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('type_of_vehicule_new') }}">Create new</a>
{% endblock %}
